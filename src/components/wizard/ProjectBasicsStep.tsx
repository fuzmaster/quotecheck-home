import { useState } from "react";import { projectTypes } from "../../data/projectTypes";import { useWizardStore } from "../../store/useWizardStore";import { projectSchema } from "../../schemas/zodProject";import { Card } from "../ui/Card";import { Input } from "../ui/Input";import { Select } from "../ui/Select";import { Button } from "../ui/Button";
export function ProjectBasicsStep(){const project=useWizardStore(s=>s.project);const updateProject=useWizardStore(s=>s.updateProject);const setStep=useWizardStore(s=>s.setStep);const[error,setError]=useState("");function submit(){const result=projectSchema.safeParse(project);if(!result.success){setError(result.error.issues[0]?.message??"Check your project details.");return}setError("");setStep(1)}return <Card><h2 className="text-2xl font-black">Project basics</h2><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="space-y-2 text-sm font-semibold">ZIP code<Input value={project.zipCode} onChange={e=>updateProject({...project,zipCode:e.target.value})}/></label><label className="space-y-2 text-sm font-semibold">Project type<Select value={project.projectType} onChange={e=>updateProject({...project,projectType:e.target.value})}><option value="">Select...</option>{projectTypes.map(t=><option key={t}>{t}</option>)}</Select></label><label className="space-y-2 text-sm font-semibold">Room or area<Input value={project.projectArea} onChange={e=>updateProject({...project,projectArea:e.target.value})}/></label><label className="space-y-2 text-sm font-semibold">Urgency<Select value={project.urgency} onChange={e=>updateProject({...project,urgency:e.target.value as typeof project.urgency})}><option value="flexible">Flexible</option><option value="soon">Soon</option><option value="urgent">Urgent</option></Select></label><label className="space-y-2 text-sm font-semibold">Permit likely?<Select value={project.permitLikely} onChange={e=>updateProject({...project,permitLikely:e.target.value as typeof project.permitLikely})}><option value="not_sure">Not sure</option><option value="yes">Yes</option><option value="no">No</option></Select></label></div>{error&&<p className="mt-4 text-sm font-bold text-red-600">{error}</p>}<div className="mt-6 flex justify-end"><Button onClick={submit}>Continue to quotes</Button></div></Card>}
