import type { RiskFlag } from "../../types/report";import { Card } from "../ui/Card";import { Badge } from "../ui/Badge";
export function MissingScopeFlags({flags}:{flags:RiskFlag[]}){return <Card className="print-card"><h2 className="text-2xl font-black">Items to clarify</h2><div className="mt-5 space-y-3">{flags.length===0?<p className="text-slate-600">No missing-information flags were generated from your entries.</p>:flags.map((f,i)=><div key={`${f.title}-${i}`} className="rounded-2xl border border-slate-200 p-4"><div className="flex flex-wrap items-center justify-between gap-3"><h3 className="font-black">{f.title}</h3><Badge>{f.level}</Badge></div><p className="mt-2 text-sm text-slate-600">{f.detail}</p></div>)}</div></Card>}
