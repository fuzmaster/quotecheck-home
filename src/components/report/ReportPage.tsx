import { DisclaimerBanner } from "../layout/DisclaimerBanner";import { useWizardStore } from "../../store/useWizardStore";import { Card } from "../ui/Card";import { Button } from "../ui/Button";import { RiskSummary } from "./RiskSummary";import { QuoteComparisonTable } from "./QuoteComparisonTable";import { MissingScopeFlags } from "./MissingScopeFlags";import { FollowUpQuestions } from "./FollowUpQuestions";import { PremiumReportLock } from "./PremiumReportLock";import { PrintReportButton } from "./PrintReportButton";
export function ReportPage(){const report=useWizardStore(s=>s.report);const reset=useWizardStore(s=>s.reset);if(!report)return null;return <div className="space-y-6"><div className="no-print"><DisclaimerBanner/></div><Card className="print-card"><div className="flex flex-wrap items-start justify-between gap-4"><div><h1 className="text-4xl font-black tracking-tight">QuoteCheck Report</h1><p className="mt-2 text-sm text-slate-600">Created {new Date(report.createdAt).toLocaleString()}</p></div><div className="no-print flex gap-3"><PrintReportButton/><Button variant="secondary" onClick={reset}>Start over</Button></div></div></Card><RiskSummary report={report}/><QuoteComparisonTable quotes={report.rankedQuotes} spread={report.spread}/><MissingScopeFlags flags={[...report.missingScope,...report.flags]}/><FollowUpQuestions questions={report.questions}/><PremiumReportLock reportId={report.reportId}/><Card className="print-card"><h2 className="text-xl font-black">Final disclaimer</h2><p className="mt-2 text-sm text-slate-600">This report is based only on the manual information entered by the user. It may miss scope issues, pricing context, code requirements, contractor qualifications, insurance requirements, hidden damage, or local permitting details. Do not treat this as legal, construction, engineering, insurance, financial, or professional advice.</p></Card></div>}
