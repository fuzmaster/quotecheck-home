import type { Quote } from "../types/quote"; import type { RiskFlag } from "../types/report";
export function generateRiskFlags(quotes:Quote[]):RiskFlag[]{const prices=quotes.map(q=>q.totalPrice);const low=Math.min(...prices);const high=Math.max(...prices);const flags:RiskFlag[]=[];for(const quote of quotes){const depositPercent=quote.totalPrice>0?(quote.depositAmount/quote.totalPrice)*100:0;if(depositPercent>=50)flags.push({quoteId:quote.id,title:`${quote.contractorName}: high deposit request`,level:"Higher Risk",detail:`Deposit is ${Math.round(depositPercent)}% of the quote. Ask what work, materials, or ordering happens before this payment is due.`});if(quote.timelineDays<=2)flags.push({quoteId:quote.id,title:`${quote.contractorName}: very short timeline`,level:"Needs Clarification",detail:"The timeline is unusually short. Confirm crew size, work days, inspection timing, and whether cleanup is included."});if(quote.totalPrice===low&&high>low*1.35)flags.push({quoteId:quote.id,title:`${quote.contractorName}: lowest bid in a wide spread`,level:"Ask Before Signing",detail:"This is the lowest quote while the overall quote spread is large. Confirm materials, permits, cleanup, warranty, and change-order triggers."});if(quote.totalPrice===high&&high>low*1.35)flags.push({quoteId:quote.id,title:`${quote.contractorName}: highest bid in a wide spread`,level:"Needs Clarification",detail:"This is the highest quote in a wide spread. Ask what extra labor, materials, warranty, or project management is included."});if(quote.materialQuality==="not_sure")flags.push({quoteId:quote.id,title:`${quote.contractorName}: material quality unclear`,level:"Missing Information",detail:"The quote does not clearly define material quality. Ask for brands, grades, allowances, or model numbers where possible."})}return flags}
